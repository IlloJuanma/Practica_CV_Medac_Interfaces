$(document).ready(function () {
    // Retira la clase después de un retardo (ajusta el tiempo según sea necesario)
    setTimeout(function () {
        $("#page-container").removeClass("fade-in");
    }, 1500); // Igual al tiempo de duración de la animación
});